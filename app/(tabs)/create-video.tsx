import { Text } from "@/components/ui/text";
import { SafeAreaView } from "react-native-safe-area-context";

const Create = () => {
	return (
		<SafeAreaView>
			<Text>Create videos</Text>
		</SafeAreaView>
	);
};

export default Create;
